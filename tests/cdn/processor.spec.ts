import { test, expect } from '@playwright/test';

// All cdn media processor tests
test.describe('CDN Processor tests', () => {
    // TODO: Implement tests
    let movieId = ''

    test('Navigate through the windows', async ({ page }) => {

    });

    test('Upload without all the metadata', ({ page }) => {

    });

    test('Upload a custom movie', ({ page }) => {

    });

    test('Check the existence of the newly created movie', ({ page }) => {

    });

    test('Edit the created movie', ({ page }) => {

    });

    test('Delete the created movie', ({ page }) => {

    });
});