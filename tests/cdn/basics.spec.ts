import { test, expect } from '@playwright/test';

// All cdn basic test
test.describe('CDN Basic Tests', () => {
    // TODO: Implement tests
    test('Not existant content URL', async ({ page }) => {

    });
    
    test('Retrieve a movie from the CDN', async ({ page }) => {

    });

    test('Retrieve a series from the CDN', async ({ page }) => {

    }); 

    test('Retrieve a profile picture from the CDN', async ({}) => {

    });

    test('Handle stress without collapsing', async ({ page }) => {

    });

    test('Low time to first byte', async ({ page }) => {

    });
});

